{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": [
    ".prettierrc.js",
    ".eslintrc.js",
    "tsconfig.json",
    "yarn.lock"
  ],
  "pipeline": {
    "start": {
      "cache": false,
      "persistent": true
    },
    "build": {
      "outputs": ["dist/**"],
      "dependsOn": ["//#tsc", "^build"]
    },
    "export-dynamic": {
      "outputs": ["dist-dynamic/**"],
      "dependsOn": ["//#tsc"]
    },
    "app#build": {
      "outputs": ["dist/**"],
      "dependsOn": ["^build"]
    },
    "clean": {
      "cache": false
    },
    "test": {
      "outputs": ["coverage/**"]
    },
    "lint": {},
    "//#prettier:fix": {
      "cache": false
    },
    "//#tsc": {
      "outputs": ["dist-types/**"],
      "dependsOn": ["^tsc"]
    }
  }
}
